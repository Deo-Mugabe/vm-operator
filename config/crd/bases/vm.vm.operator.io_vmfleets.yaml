---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: vmfleets.vm.vm.operator.io
spec:
  group: vm.vm.operator.io
  names:
    kind: VmFleet
    listKind: VmFleetList
    plural: vmfleets
    shortNames:
    - vf
    singular: vmfleet
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.currentReplicas
      name: Current
      type: integer
    - jsonPath: .spec.replicas
      name: Desired
      type: integer
    - jsonPath: .spec.cpu
      name: CPU
      type: integer
    - jsonPath: .spec.memory
      name: Memory
      type: integer
    - jsonPath: .spec.diskGB
      name: DiskGB
      type: integer
    - jsonPath: .spec.template
      name: Template
      type: string
    - jsonPath: .status.lastMessage
      name: Message
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VmFleet is the Schema for the vmfleets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VmFleet - what you want
            properties:
              cpu:
                format: int32
                maximum: 4
                minimum: 1
                type: integer
              diskGB:
                format: int32
                maximum: 100
                minimum: 10
                type: integer
              memory:
                format: int32
                maximum: 8
                minimum: 1
                type: integer
              replicas:
                format: int32
                minimum: 1
                type: integer
              template:
                type: string
            required:
            - cpu
            - diskGB
            - memory
            - replicas
            - template
            type: object
          status:
            description: status defines the observed state of VmFleet -  what exists
            properties:
              currentReplicas:
                format: int32
                type: integer
              desiredReplicas:
                format: int32
                type: integer
              lastMessage:
                type: string
              phase:
                description: |-
                  conditions represent the current state of the VmFleet resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state
                type: string
            required:
            - desiredReplicas
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
